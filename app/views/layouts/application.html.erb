<!DOCTYPE html>
<html>
  <head>
    <title>Adrian's WikiRoR Store</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header class='main'>
      <%= image_tag 'logo.png', alt: "Adrian's WikiRoR Store", class: "mainStoreLogo" %>
      <h1><%= @page_title %></h1>
        <% if @counter && @counter >= 5 %>
        <aside class='sassyAside'>You've been here <%= pluralize(@counter, 'time', 'times')%> and haven't put anything in your cart. What gives?</aside>
        <% else %>
        <aside class='sassyAside'>Buy something, will ya?</aside>
        <% end %>
    </header>
    <section class='content'>
      <nav class='side_nav'>
        <ul>
          <li><a href="/">Home</a></li>
          <li><a href="/questions">Questions</a></li>
          <li><a href="/news">News</a></li>
          <li><a href="/contact">Contact</a></li>
        </ul>
        <strong class='side_nav_strong'><%= Time.now.strftime("%m/%d/%Y") %></strong>
      </nav><!--side nav-->
      <main class='<%=controller.controller_name %>'>
      <%= yield %>
      <!--The real magic of this markup is on the line above, if there is
      page specific content generated by the view invoked by the request/??path??
      then we will see THAT instead of this.-->
      </main>
    </section>
  </body>
</html>
